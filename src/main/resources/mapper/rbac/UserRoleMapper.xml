<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.opsflow.rbac.mapper.UserRoleMapper">

    <resultMap id="BaseResultMap" type="org.example.opsflow.rbac.entity.UserRole">
            <id property="id" column="id" />
            <result property="userId" column="user_id" />
            <result property="roleId" column="role_id" />
            <result property="createdAt" column="created_at" />
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,role_id,created_at
    </sql>
    <insert id="batchInsert">
        INSERT INTO sys_user_role(user_id, role_id)
        VALUES
        <foreach collection="roleIds"
                 item="roleId"
                 separator=",">
                (#{userId},#{roleId})
        </foreach>
    </insert>
    <delete id="deleteByUserId">
        DELETE FROM sys_user_role
        WHERE user_id = #{userId}
    </delete>
    <select id="findEnabledByUserId" resultType="org.example.opsflow.rbac.entity.Role">
        SELECT r.id,r.code,r.name,r.description,r.status,r.created_at,r.updated_at
        FROM sys_role r
        JOIN sys_user_role ur
            ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
        AND r.status = 1
        ORDER BY r.id
    </select>
    <select id="findAssignedByUserId" resultType="org.example.opsflow.rbac.entity.Role">
        SELECT r.id,r.code,r.name,r.description,r.status,r.created_at,r.updated_at
        FROM sys_role r
        JOIN sys_user_role ur
            ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
        ORDER BY r.id
    </select>
</mapper>
